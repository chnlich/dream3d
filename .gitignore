# Dependencies
node_modules/

# Build output
dist/

# Local secrets / config — keys are supplied at runtime, never committed
config/local.json

# Smoke-test downloads
scripts/.out/

# Sample GLB assets (~42MB) — gitignored; fresh clones regenerate via scripts/meshy-generate.mjs
public/sample-assets/

# OS / editor cruft
.DS_Store
.opencode/
